server:
  port: 5004
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri:
          search-paths: config-data
  application:
    name: config-service
